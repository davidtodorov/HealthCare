/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { AppointmentModel } from '../../models/appointment-model';

export interface AppointmentGetCurrentPatientAppointments$Params {
}

export function appointmentGetCurrentPatientAppointments(http: HttpClient, rootUrl: string, params?: AppointmentGetCurrentPatientAppointments$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<AppointmentModel>>> {
  const rb = new RequestBuilder(rootUrl, appointmentGetCurrentPatientAppointments.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<AppointmentModel>>;
    })
  );
}

appointmentGetCurrentPatientAppointments.PATH = '/api/Appointment/GetCurrentPatientAppointments';
