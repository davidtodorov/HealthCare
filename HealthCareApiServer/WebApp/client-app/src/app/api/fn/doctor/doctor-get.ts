/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { DoctorModel } from '../../models/doctor-model';

export interface DoctorGet$Params {
  id: number;
}

export function doctorGet(http: HttpClient, rootUrl: string, params: DoctorGet$Params, context?: HttpContext): Observable<StrictHttpResponse<DoctorModel>> {
  const rb = new RequestBuilder(rootUrl, doctorGet.PATH, 'get');
  if (params) {
    rb.path('id', params.id, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<DoctorModel>;
    })
  );
}

doctorGet.PATH = '/api/Doctor/{id}';
