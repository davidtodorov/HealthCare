/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { DoctorModel } from '../../models/doctor-model';

export interface DoctorGetAll$Params {
}

export function doctorGetAll(http: HttpClient, rootUrl: string, params?: DoctorGetAll$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<DoctorModel>>> {
  const rb = new RequestBuilder(rootUrl, doctorGetAll.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<DoctorModel>>;
    })
  );
}

doctorGetAll.PATH = '/api/Doctor';
