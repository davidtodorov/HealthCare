/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { HospitalModel } from '../../models/hospital-model';

export interface ApiHospitalIdGet$Json$Params {
  id: number;
}

export function apiHospitalIdGet$Json(http: HttpClient, rootUrl: string, params: ApiHospitalIdGet$Json$Params, context?: HttpContext): Observable<StrictHttpResponse<HospitalModel>> {
  const rb = new RequestBuilder(rootUrl, apiHospitalIdGet$Json.PATH, 'get');
  if (params) {
    rb.path('id', params.id, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'text/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<HospitalModel>;
    })
  );
}

apiHospitalIdGet$Json.PATH = '/api/Hospital/{id}';
